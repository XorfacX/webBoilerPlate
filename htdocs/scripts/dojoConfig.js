var dojoConfig = {
    baseUrl: "./scripts/dojo/", // the folder where dojo.js lives --> default to current path
    async: 1, //AMD loader
    selectorEngine: 'css3',
    waitSeconds: 4,
    paths: { "base": "../", "app": "../app/" }
};